  {{!-- {{#each playDates as |playdate| }}
<div id="event_container" class="d-flex flex-wrap m-5 p-2 ">
  <div class="card rounded text-center" style="width: 18rem;">
    <img class="card-img-top" src="{{pet.image}}" alt="{{pet.pet_name}}">
    <div class="card-body">
      <h5 class="card-title">{{pet.pet_name}}</h5>
      <p class="card-text">
        {{pet.Age}} <br> {{pet.breed}}
      </p>
      <a href="{{Event:ID}}" class="btn btn-primary">View Details</a>
    </div>
  </div>

</div>
{{/each}} --}}
{{#each playDates as |playdate| }}
<div class="card m-5">
  <h5><div class="card-header bg-success text-dark text-center">Event on {{playdate.date}} at {{playdate.location}}</div></h5>
  <div class="card-body row justify-content-center">
    {{#each playdate.pets as |pet|}}
      <img class="card-img-top rounded-1 profileImage border border-success" src="{{pet.image}}" alt="{{pet.pet_name}}">
    {{/each}}
  </div>
    <a href="/event/{{playdate.id}}" class="btn btn-primary bg-success text-dark mx-auto w-25 m-3">View Details</a>
</div>
{{/each}}